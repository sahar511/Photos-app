import { FilterMenuItemsPipe } from './filter-menu-items.pipe';

describe('FilterMenuItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMenuItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
